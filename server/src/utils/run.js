if ([]) {
  console.log("Happy Hacking");
}
console.log("Happy Coding");


 
/*
 * This is deprecated , This package is  no longer supported. It's now a built-in Node module. If you've depended on crypto, 
you should switch to the one that's built-in.
 */

npm install crypto

// Use the built-in Node module instead
const crypto = require('crypto')


npm install @types/mongoose

// Mongoose publishes its own types, so you do not need to install this package.